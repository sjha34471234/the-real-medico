/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'images.printify.com',
      'storage.googleapis.com',
      'via.placeholder.com'
    ],
  },
}

module.exports = nextConfig
